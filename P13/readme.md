### P13  要清楚，啥时候会调用构造函数，啥时候是赋值语句
#### 转换构造函数
    单个参数的构造函数，把其他类型转为类类型
#### =重载
    将operator=看做函数名
#### explicit
    写在构造函数前，即表示，不允许隐式构造函数（用  =  调用 构造函数, 包括：
        1.test t3  = 40；

        2.test t1(10);
          t1 = 20;  
        ）
